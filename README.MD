@"
# MIS-690 Capstone — Target Guest Segmentation

K-Means clustering on RFM features using the Olist Brazilian E-Commerce dataset as a proxy for Target's internal guest transaction data.

## Project Structure
- ``data/`` — Olist raw CSVs (excluded from git, download from Kaggle)
- ``notebooks/`` — Jupyter notebooks by topic
- ``outputs/`` — Generated charts and Excel deliverables

## Topics
- Topic 2: Data Understanding, Acquisition & Preprocessing
"@ | Out-File -FilePath README.md -Encoding utf8